import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    bodyContainer: {
        flex: 1,
        flexDirection: 'column',
        alignItems: 'stretch',
        padding: 20,
    }
});